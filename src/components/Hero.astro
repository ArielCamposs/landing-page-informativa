<section
    class="relative h-screen flex items-center justify-center overflow-hidden bg-slate-900"
>
    <!-- Imagen de fondo con overlay -->
    <div
        class="absolute inset-0 opacity-40 bg-[url('https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=2070&auto=format&fit=crop')] bg-cover bg-center"
    >
    </div>

    <div class="relative z-10 container mx-auto px-4 text-center text-white">
        <span
            id="hero-subtitle"
            class="block text-blue-400 font-semibold tracking-widest mb-4 opacity-0 translate-y-4"
            >NUEVA COLECCIÓN 2025</span
        >
        <h1
            id="hero-title"
            class="text-5xl md:text-7xl font-bold mb-6 leading-tight opacity-0 translate-y-8"
        >
            Diseño que Ilumina <br /> Tu Espacio
        </h1>
        <p
            id="hero-text"
            class="text-lg md:text-xl text-slate-300 max-w-2xl mx-auto mb-10 opacity-0 translate-y-8"
        >
            Descubre piezas únicas que transforman cualquier habitación.
            Minimalismo y funcionalidad en perfecta armonía.
        </p>

        <div
            id="hero-buttons"
            class="flex flex-col md:flex-row gap-4 justify-center opacity-0 translate-y-8"
        >
            <a
                href="#catalogo"
                class="bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-full font-semibold transition-all hover:scale-105 hover:shadow-lg hover:shadow-blue-500/30"
            >
                Ver Catálogo
            </a>
            <a
                href="#contacto"
                class="bg-transparent border border-white hover:bg-white hover:text-slate-900 text-white px-8 py-4 rounded-full font-semibold transition-all hover:scale-105"
            >
                Contáctanos
            </a>
        </div>
    </div>
</section>

<script>
    import { animate, stagger } from "motion";

    // Animación de secuencia al cargar
    animate(
        ["#hero-subtitle", "#hero-title", "#hero-text", "#hero-buttons"],
        { opacity: [0, 1], y: [30, 0] },
        { duration: 0.8, delay: stagger(0.2) },
    );
</script>
